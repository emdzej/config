apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "config-service.configmap.configs" . }}
  labels:
    {{- include "config-service.labels" . | nindent 4 }}
data:
  {{- range $filename, $content := .Values.configFiles }}
  {{ $filename }}: |
    {{- $content | nindent 4 }}
  {{- end }}
