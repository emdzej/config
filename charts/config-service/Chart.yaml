apiVersion: v2
name: config-service
description: Centralised read-only config service with placeholder resolution, env overlay, and JSON Schema validation
type: application
version: 0.1.0
appVersion: "0.1.0"
keywords:
  - config
  - configuration
  - runtime-config
  - json-schema
maintainers:
  - name: emdzej
